/*
function aleatorio() {
    return Math.random()
}

function cuadrado(x,y) {
    return x * x;
}

function factoria(nombre, apellidos) {
    return {nombre, apellidos};
}

Crea versiones arrow de las funciones anteriores.
- Con paréntesis y sin paréntesis si es posible.
- Con llaves en el cuerpo y sin llaves si es posible.
- Invoca (llama) las funciones.
*/

/*
Crea una función "creaPersona" con parámetros opcionales y valores por defecto
parámetros:
- nombre y apellidos (obligatorio) Lanzar excepción si no se pasan: throw new TypeError('nombre y apellidos obligatorios');
- fecha de nacimiento opcional. Si el parámetro no se pasa no se añade la propiedad al objeto persona. 
- ciudad. Valor por defecto Jaén.
- La función debe retornar un objeto con las propiedades.
*/

/*
Crea una función que calcule la media de los parámetros pasados.
Pasa un array a la función con el operador spread
*/


/*
Crea la función imprime a la que se le pasa un objeto
como el siguiente:

imprime({nombre:"Susana", apellidos:"Pérez", edad:12});

En los parámetros de la función desestructura nombre y apellidos e imprímelos con console.log

Crea la siguiente función que suma los dos primeros elementos de un array:
sumaPrimeroSegundo([10,20,30,40,50,60]);
Desestructura los dos primeros elementos en la variable x e y.
*/

/*
Crea un objeto con la propiedad ancho y alto y el método toString.
Crea un segundo objeto con valores distintos de ancho y alto. No se implementa toString.
Haz un call para usar el método toString del primer objeto en el segundo.
*/





